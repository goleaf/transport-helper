<span class="inline-flex items-center gap-2" title="{{ $description }}">
    <x-supply.badge tone="{{ $tone }}">{{ $label }}</x-supply.badge>
    <span class="text-sm text-slate-600">{{ $percent }}</span>
</span>
